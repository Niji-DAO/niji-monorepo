node_modules
.vscode
build/
dist
/**/sha.ts
/**/sha.h
*.tgz
# env file 群 (3 環境 pattern = .env.local / .env.dev / .env.prod + Vite native .env.local)
# .env.*.example のみ tracked、 実 secret 含む .env / .env.local / .env.dev / .env.prod は gitignored
.env
.env.local
.env.dev
.env.prod
.env.development
.env.production
!.env.*.example
.DS_Store
*.tsbuildinfo
*.log
.claude

# Local Netlify folder
.netlify

.turbo
.cache
i18n.cache

# session scratch (Claude Code, local-only working dir)
.context/

# niji deploy artifacts (deploy log JSON kept selectively, but render PNG / SVG / metadata are large)
packages/niji-contracts/deploy/

# deployments snapshots: keep mainnet / sepolia / base-sepolia tracked, ignore local dev snapshots
packages/niji-contracts/deployments/localhost.json
packages/niji-contracts/deployments/hardhat.json

# generated test outputs / playwright traces / forge artifacts
packages/niji-assets/test_output/
packages/niji-webapp/test-results/
packages/niji-contracts/test-results/

# foundry forge script --broadcast の自動生成 deploy 履歴 (niji deploy は packages/niji-contracts/deploy/ で別管理)
packages/niji-contracts/broadcast/
