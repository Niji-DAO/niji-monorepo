name: Forge (contracts) CI

on:
  workflow_dispatch:
  push:
    branches:
      - master
      - 'feature/*'
    paths:
      - 'packages/niji-contracts/**'
  pull_request:
    paths:
      - 'packages/niji-contracts/**'
    branches:
      - '**'

env:
  NODE_OPTIONS: --max_old_space_size=5120

jobs:
  build_and_test:
    name: Build and Test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22.x

      - name: Setup pnpm
        uses: pnpm/action-setup@v4

      - name: Cache dependencies
        uses: actions/cache@v4
        with:
          path: |
            **/node_modules
            ~/.pnpm-store
          key: ${{ runner.os }}-pnpm-${{ hashFiles('**/pnpm-lock.yaml') }}
          restore-keys: |
            ${{ runner.os }}-pnpm-

      - name: Install dependencies
        run: pnpm install --filter @niji/contracts

      - name: Cache Foundry build
        uses: actions/cache@v4
        with:
          path: |
            packages/niji-contracts/out
            packages/niji-contracts/cache
          key: ${{ runner.os }}-foundry-${{ hashFiles('packages/niji-contracts/foundry.toml', 'packages/niji-contracts/src/**', 'packages/niji-contracts/test/**') }}

      - name: Install Foundry
        uses: foundry-rs/foundry-toolchain@v1
        with:
          version: stable

      - name: Run Forge build
        run: |
          cd packages/niji-contracts
          forge --version
          forge build --optimize --optimizer-runs 1 --sizes --skip test

      - name: Cache Foundry test data
        uses: actions/cache@v4
        with:
          path: |
            packages/niji-contracts/out 
            packages/niji-contracts/cache
            ~/.foundry/cache
          key: ${{ runner.os }}-foundry-${{ hashFiles('packages/niji-contracts/foundry.toml', 'packages/niji-contracts/src/**', 'packages/niji-contracts/test/**', '~/.foundry/cache/**') }}

      - name: Run Forge tests
        env:
          RPC_MAINNET: ${{ secrets.RPC_MAINNET != '' && secrets.RPC_MAINNET || 'https://ethereum-rpc.publicnode.com' }}
        run: |
          cd packages/niji-contracts
          forge test -vvv --ffi
